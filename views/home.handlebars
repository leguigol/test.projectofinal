<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <title>Home de Productos</title>
</head>
<body class="container mt-5">
    <h1 class="mb-4">Lista de Productos</h1>
    <ul class="list-group">
        {{#each productos}}
            <li class="list-group-item">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <span class="fw-bold">ID:</span> {{this._id}} <br>
                        <span class="fw-bold">Título:</span> {{this.title}} <br>
                        <span class="fw-bold">Descripción:</span> {{this.description}} <br>
                        <span class="fw-bold">Precio:</span> {{this.price}}
                    </div>
                    <!-- Puedes agregar más detalles o acciones aquí, como botones de edición o eliminación -->
                </div>
            </li>
        {{/each}}
    </ul>
</body>
</html>